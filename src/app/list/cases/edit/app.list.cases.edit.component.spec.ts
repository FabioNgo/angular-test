import { TestBed, async } from '@angular/core/testing';
import { AppCasesComponent } from './app.list.cases.edit.component';
describe('AppLoginComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        AppCasesComponent
      ],
    }).compileComponents();
  }));
});

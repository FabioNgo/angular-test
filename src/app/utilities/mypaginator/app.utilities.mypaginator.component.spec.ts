import { TestBed, async } from '@angular/core/testing';
import {MyPaginatorComponent} from './app.utilities.mypaginator.component';
describe('AppLoginComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        MyPaginatorComponent
      ],
    }).compileComponents();
  }));
});
